# ngntun 設計ドキュメント

NTT フレッツの**データコネクト**を Linux から張るコマンドの設計と、実機検証で分かったこと。

このドキュメントに載っているプロトコルの挙動は、すべて実機の HGW で確認したもの。
公開にあたり、アドレスと MAC は例示用の値（RFC 3849 の `2001:db8::/32`、RFC 7042 の文書用 MAC）に、
電話番号は伏せ字（自局 `03-XXXX-XXXX` / 相手 `03-YYYY-YYYY`）に置き換えてある。

---

## 1. データコネクトの仕様（実測でわかっていること）

データコネクトは、ひかり電話の呼制御を使って拠点間に帯域確保型のデータセッションを張るサービス。
**SIP で相手の電話番号に発信し、SDP で交渉した UDP フローの中に生の IP パケットを流す**、という構造になっている。

| 層 | 内容 |
|---|---|
| プロビジョニング | HGW の DHCPv6 が SIP サーバ（Option 22）、NTT ベンダオプション（Enterprise 210: code 201=クライアント MAC / 202=内線番号 / 204=SIP ドメイン）、委譲プレフィックス（IA_PD）を配布 |
| 登録 | `REGISTER sip:<ドメイン>`、From/To とも `sip:<内線>@<ドメイン>`、Contact に IPv4 と IPv6 の両方、Expires 3600、認証チャレンジなしで即 200 OK |
| 発信 | IPv6 の SIP/UDP 5060。`INVITE sip:<相手番号>@<ドメイン>`、`Route: <sip:[HGW];lr>`、発番は `P-Preferred-Identity`、`Supported: 100rel,timer`、`Session-Expires: 300`（200 OK 側で `refresher=uas`） |
| SDP | `m=application <port> udp octet-stream` / `b=AS:64` / `a=udp-setup:active` |
| メディア | UDP ペイロードが**追加ヘッダなしの生 IP パケット**。宛先は相手拠点ではなく NGN 網内のメディアリレー（NTT の網設備ブロック `2404:1a8::/32`）で、エンドツーエンドではない |
| QoS | SIP は DSCP EF(0xb8)、メディア送信は CS1(0x20)。網からの受信は CS4 にリマークされて返る |
| 切断 | 無通信が続くと自機から BYE（従量課金なので自動切断が課金制御の要） |

### 1.1 実装が依拠する決定事項

1. **REGISTER は IPv4 で行う。** 実機の HGW は IPv6 の REGISTER に一切応答しなかった（§6.3）。
   したがって UA は **IPv4 と IPv6 のトランスポートを両方持ち、登録は IPv4・呼は IPv6** という非対称な構成になる。
2. **Digest 認証は実装しない。** 検証環境では 401/407 のチャレンジは一切なく、即 200 OK が返る。
   チャレンジが来た場合は、黙って失敗せず「この HGW は認証を要求している」と明示してエラー終了する。
3. **内線番号は DHCPv6 のベンダオプション（code 202）の値をそのまま使う。**
   HGW 側のバインドは DHCPv6 クライアントの **DUID** に紐づく（4.1 で実測）前提とし、内線番号の交渉や探索は行わない。
   運用上は dhclient の DUID を固定することだけ注意する。

---

## 2. アーキテクチャ

```
                    ┌──────────────────── ngntun (1 プロセス) ────────────────────┐
                    │                                                            │
 /run/ngntun/       │  ┌────────┐   ┌──────────────┐   ┌─────────┐   ┌─────────┐ │
 lease{6,4}.json ──►│  │ lease  │──►│ sip (UA)     │──►│ session │──►│ netcfg  │ │
                    │  │ parser │   │ REGISTER/    │   │ 状態機械 │   │ netlink │ │
                    │  └────────┘   │ INVITE/BYE   │   │ タイマ群 │   └────┬────┘ │
                    │               └──────┬───────┘   └────┬────┘        │      │
                    │                      │ SDP answer     │ 確立/切断    │      │
                    │                      ▼                ▼             ▼      │
                    │        ┌────────────────────────────────────────────────┐  │
                    │        │ media: UDP socket ⇔ tun (双方向 goroutine)      │  │
                    │        └────────┬──────────────────────────┬────────────┘  │
                    └─────────────────┼──────────────────────────┼───────────────┘
                                      │                          │
    REGISTER: SIP/UDP over IPv4 ──────┤                          ├── /dev/net/tun
    INVITE 以降: SIP/UDP over IPv6    │                          │   dc0
                                      ▼                          ▼
                            ┌──────────────────┐        ┌──────────────────┐
                            │ HGW (SIP proxy)  │        │ カーネル経路表    │
                            └────────┬─────────┘        └──────────────────┘
                                     │
                            ┌────────▼─────────┐
                            │ NGN 網内メディア  │  ← メディアは常にここ宛
                            │ リレー           │
                            └──────────────────┘
```

プロセスの責務は「SIP で呼を作り、**その呼の生存期間だけ tun を存在させる**」こと。
呼が消えたら tun も経路も消える、という 1 対 1 対応にしておくと状態管理が破綻しない。

---

## 3. 言語と SIP スタックの選択

### Go を選んだ理由

- **netlink と tun の両方を素で扱える。** `vishvananda/netlink` は枯れているし、
  tun は `/dev/net/tun` の ioctl 2 発なので外部依存なしで書ける
- **単一バイナリで配布できる。** cgo なしでクロスコンパイルでき、実験機に scp 1 回で置ける
- **並行処理が素直。** tun 読み / UDP 読み / SIP 受信 / 各種タイマを goroutine + `context` で書くと、状態機械が読みやすい
- ソケットオプション（`IPV6_TCLASS` での DSCP 指定、`SO_BINDTODEVICE`）は `golang.org/x/sys/unix` でそのまま叩ける

GC の遅延は 64 kbps〜1 Mbps の帯域では完全に無視できる。

### SIP ライブラリを使わず自前実装にした理由

- 相手が**特定の HGW 1 機種**で、必要なメソッドは REGISTER / INVITE / ACK / BYE / UPDATE / OPTIONS / PRACK だけ。ダイアログも常に 1 本
- 必要なメッセージの形が**実機検証で確定している**ので、それに合わせて書くのが最短かつ最も確実
- `m=application udp octet-stream` や `a=udp-setup` といった非標準寄りの SDP、`P-Preferred-Identity` の付与、
  そして §6.2 の Contact トークンのような細かい辻褄合わせを、ライブラリの抽象と戦わずに扱える

ただし RFC 3261 のトランザクション層（タイマと再送）は手を抜いていない。
`internal/sip` のインタフェースは薄く保ってあるので、必要になればライブラリに差し替えられる。

---

## 4. リース情報の受け渡し

### 4.1 Vendor Class を送らないと内線番号が来ない

**これが最大の要点。** DHCPv6 の Vendor Class（option 16）を送らないと、HGW は SIP サーバと
委譲プレフィックスだけを返し、ベンダオプション（内線番号・SIP ドメイン）を一切返さない。

HGW が受け付ける option 16 の中身:

```
00 10 | 00 0c | 00 00 04 9e | 00 06 | <6 バイトの MAC アドレス>
 code   len     enterprise    len
                1182
```

「enterprise-number + 長さ + 自分の MAC アドレス」という構造。HGW の web UI での事前登録は
不要で、この option を送るだけで内線番号が自動的に割り当てられる。

**引き金はオプションを送ること自体で、中身は検証されていない。** 実機で 3 つの MAC
（L2 フレームの実 MAC / DUID 内の LL アドレス / Vendor Class に書いた MAC）を別々の値にして
確かめた。

| どこ | 値 |
|---|---|
| 実 MAC | `a0:ce:c8:cf:1d:ae` |
| DUID (Client-ID) 内 | `02:11:33:44:55:66` |
| Vendor Class | `02:4e:8f:1c:6b:d3` |

結果は `code 201 = 02:11:33:44:55:66`、つまり **DUID の値**が返り、内線番号は新規に払い出され、
REGISTER も通った。Vendor Class の値は返ってこず、L2 の実 MAC も使われない。Vendor Class だけを
架空の値にして DUID を保った場合は、内線番号は元のまま変わらなかった。

したがって **`code 201` は Vendor Class のエコーではなく DUID 内の MAC**、**内線番号を紐づけて
いる識別子は DUID** である。dhclient の DUID-LLT はその IF の MAC から作られるので、普段は
3 つが偶然一致して見える。運用上は DUID を消さないことだけ注意すればよい（消すと別端末とみなされ、
新しい内線番号を消費する）。

中身が検証されていなくてもこの形で送っているのは、**対応ルーター (RTX810) が実際に送っていた
ものに合わせている**ため。将来ファームウェアが検証を始めたときに壊れないよう、実機に倣った形を保つ。

ISC dhclient は `dhcp6.vendor-class` を定義していないため、生バイト列として自分で定義する必要がある。

```
option dhcp6.vendor-class code 16 = string;
send dhcp6.vendor-class 00:00:04:9e:00:06:00:00:5e:00:53:22;
```

定義を忘れて `send` すると、dhclient は
`no option named vendor-class in space dhcp6` を出しつつ**そのまま送信を続けてしまう**ため、
「なぜかベンダオプションが返ってこない」という形でしか気づけない。

### 4.2 リース取得に経路をいじらせない

`dhclient-exit-hooks.d` に置く方式ではなく、`-sf` で**経路に一切触らないスクリプト**を渡す。
標準の dhclient-script はデフォルトゲートウェイを書き換えるため、既にインターネット側の
経路を持つ機器で走らせると疎通が切れる。
これにより NetworkManager など既存のネットワーク管理と並走させても安全になる。

ただしこの「安全」は片側だけで、**逆向きは成り立たない**。NetworkManager は `nmcli device reapply`・
再接続・再起動のたびに、自分の管理外であるこのアドレスを消す。消されない工夫をするより、
消されたら付け直す側に倒す。

そのため常駐は **NM の dispatcher から起動する**。systemd の順序制御 (`After=` や
`network-online.target`) で解けるのは「起動が早すぎて Solicit を出せない」ほうだけで、
「後から NM に消される」ほうは順序では勝てないため。dispatcher は NM がインタフェースを
上げた後に呼ばれるので、どちらの順序でも辻褄が合う。`up` に加えて `reapply` も拾う必要がある
(`nmcli device reapply` はインタフェースを落とさないままアドレスだけ消していく)。

スクリプトが行うのは次の 2 つだけ。

1. リースの中身を JSON に書き出す
2. 委譲プレフィックスの先頭 /64 に EUI-64 で送信元アドレスを付ける

2 は「経路に触らない」という線引きの内側に収まる。デフォルトゲートウェイを壊す危険がなく、一方でこれを人手に残すと、プレフィックスが変わったときに古いアドレスが残って 6.5 を踏む。

付けたアドレスは `/run/ngntun/source-addr6` に控え、変わっていれば消してから付け直す。`old_ip6_prefix` に頼らないのは、dhclient を起動し直すとそれが渡ってこないため。`DEPREF6` / `EXPIRE6` / `RELEASE6` / `STOP6` でも消す。消しておけば ngntun は「委譲プレフィックス内のアドレスが無い」と明示的に失敗するので、黙って捨てられるより扱いやすい。自前で管理したい環境のために `NGNTUN_SKIP_ADDR=1` で無効化できる。

ベンダオプションの TLV 解析は Go 側で行う。シェルに解析ロジックを持たせるとデバッグできなくなるため。

```
Option 17 (vendor-opts) payload:
  [0:4]  enterprise-number = 210 (NTT)
  以降、サブオプションの TLV:
    code(2) len(2) value(len)
      201 -> クライアント MAC (6 bytes)
      202 -> 内線番号 (ASCII, 例 "3")
      204 -> SIP ドメイン (DNS 名前形式)
```

未知の code は読み飛ばす（将来ファームウェア更新で増える可能性がある）。
dhclient は hex を**先頭の 0 を省いたコロン区切り**（`0:0:0:d2`）で渡してくるので、
デコーダはこの形と `00:00:00:d2` 形式、コロンなしの連結形式のすべてを受け付ける。

### 4.3 リースから決まるものはフラグに書かせない

SIP サーバ・SIP ドメイン・内線番号・委譲プレフィックス・IPv4 アドレスはすべてリースから取れる。これらを**コマンドラインに書かせないこと**を原則にする。書かせると、リースが更新されたときにコマンドラインだけ古い値が残り、6.5 のように「登録はできるが発信だけ黙殺される」形で顕在化する。

送信元 IPv6 だけはリースに直接入っていないが、`--wan-interface` 上のグローバルアドレスから**委譲プレフィックスに含まれるもの**を選べば一意に決まる。HGW と同じ /64 のオンリンク側アドレスが同居していても候補から外れるので、多ホーム環境でも取り違えない。委譲プレフィックス内に候補が無い場合は、黙って別のアドレスを使わずエラーにする。

ここで決まるのは送信元だけで、**HGW へ届く経路は範囲外**である。スクリプトは経路に触らず、ngntun も自分では足さない。RA を受けている構成なら降ってくるので表面化しないが、そうでない機器では送信元アドレスは正しいのに HGW へ届かない。環境側で用意してもらう前提であり、README 1.6 に手当てを書いた。

**IPv4 の送信元も同じ方針にする。** `--wan-interface` に実際に付いている IPv4 から、レジストラ (HGW) と同じサブネットにあるものを選ぶ。DHCPv4 リースの `ip_address` はフォールバックに留める。理由は、リースを書いた DHCP クライアントと、インタフェースにアドレスを付けている主体が別になりうるからである。両者は別のクライアントとして DHCP を引くので**別のアドレスがリースされ**、リースの値を信じると `bind: cannot assign requested address` になる。実機 (NetworkManager が .7、ngntun の dhclient が .8) で踏んだ。

結果として **DHCPv4 のリースは任意**になった。そこから使う値は REGISTER の宛先だけで、それは `--registrar4` で足りる。リースファイルが無いことは警告しない (壊れている場合だけ知らせる)。足りなければ `Validate` が具体的に報告する。

結果として、利用者が指定するのは相手の電話番号とトンネルの設定だけになる。`--sip-server` / `--registrar4` / `--sip-domain` / `--extension` / `--source-addr` / `--source-addr4` という上書きフラグは一通り持つが、これらが要るのはリースの値が実態と食い違うときと、リースファイルなしで動かすときだけ。

---

## 5. 内部設計

### 5.1 パッケージ構成

```
cmd/ngntun/main.go   フラグ解析、シグナル処理、終了コード
internal/lease/      リース JSON の読み込みとベンダオプションの TLV デコード
internal/sip/        トランスポート / トランザクション / ダイアログ / メッセージ
internal/sdp/        オファー生成・アンサー解析（m=application udp octet-stream）
internal/session/    状態機械。SIP と media と netcfg を束ね、確立後のコマンドも管理する
internal/media/      メディア UDP ソケットと tun の橋渡し、統計、無通信検出
internal/sockopt/    DSCP マーキングと SO_BINDTODEVICE
internal/tundev/     /dev/net/tun の ioctl による生成
internal/netcfg/     netlink によるアドレス・経路設定とロールバック
```

### 5.2 トランスポートの使い分け

| 用途 | トランスポート | 相手 |
|---|---|---|
| REGISTER / 登録更新 | IPv4 UDP 5060 | HGW の IPv4（既定はデフォルトゲートウェイ） |
| INVITE / ACK / BYE / UPDATE | IPv6 UDP 5060 | HGW の IPv6（DHCPv6 Option 22） |
| メディア | IPv6 UDP | NGN 網内リレー（SDP アンサーのアドレス） |

Contact は IPv4・IPv6 の両方を登録する。**着信は IPv6 Contact に来る**ため両方必須。
Call-ID / CSeq 空間は登録用とダイアログ用で独立させる。

### 5.3 状態機械

```
Init（tun 作成）→ Registering → Registered → Calling → Established →（コマンド実行）
                                                          │
      無通信 / 最大保持 / シグナル / 相手 BYE / メディア断 / コマンド終了 ─┘
                          ↓
      Cleanup: コマンド終了要求 → BYE → 経路削除 → tun 削除 → 登録解除
```

tun は**呼を張る前に**作る。権限不足などはここで落としたい（課金の発生する発信を済ませてから失敗するのは避ける）。
逆にアドレスと経路は呼が確立してから入れ、終了時に必ず巻き戻す。

後始末は `defer` に散らさず明示的な単一の順序で実行し、どの経路から来ても同じ手順を通す。
**BYE を最初に出す**のがこの順序の要点で、課金が止まるのは BYE が届いた時点だから、
経路や tun の巻き戻しを待たせない。BYE は最大 2 秒でタイムアウトさせ、応答が来なくても後始末は続行する。

### 5.4 タイマ

実装漏れが事故に直結するので一覧で管理する。

| タイマ | 値 | 動作 |
|---|---|---|
| SIP T1 / T2 | 500 ms / 4 s | 再送間隔（指数バックオフ） |
| Timer B / F | 32 s | INVITE / 非 INVITE トランザクションのタイムアウト |
| 登録更新 | Expires の 1/2 | 再 REGISTER |
| セッションタイマ | `Session-Expires: 300`、`refresher=uas` | **網側が更新**するので re-INVITE / UPDATE を受けたら同一 SDP で 200 OK を返す。更新が来ないまま期限を過ぎたら自ら切断 |
| 無通信監視 | `--idle-timeout` | 双方向とも無通信の時間で判定 |
| 最大保持 | `--max-duration` | 超過で強制 BYE |
| コマンドの終了猶予 | 5 s | 呼の終了で `SIGTERM`、過ぎたら `SIGKILL` |

### 5.5 SIP メッセージの形

`{{.ContactUser}}` は**内線番号ではなく**、UA インスタンスごとに生成するランダムな 12 桁の
16 進トークン（理由は §6.2）。REGISTER・INVITE・ダイアログ内リクエストで同じ値を使う。

**REGISTER**（IPv4 で HGW へ）

```
REGISTER sip:{{.Domain}} SIP/2.0
Via: SIP/2.0/UDP {{.SelfV4}}:5060;branch=z9hG4bK{{.Branch}};rport
Max-Forwards: 70
From: <sip:{{.Extension}}@{{.Domain}}>;tag={{.FromTag}}
To: <sip:{{.Extension}}@{{.Domain}}>
Call-ID: {{.CallID}}
CSeq: {{.CSeq}} REGISTER
Contact: <sip:{{.ContactUser}}@{{.SelfV4}}:5060>
Contact: <sip:{{.ContactUser}}@[{{.SelfV6}}]:5060>
Expires: 3600
Allow: INVITE,CANCEL,ACK,BYE,UPDATE,PRACK
Supported: path
Content-Length: 0
```

**INVITE**（IPv6 で HGW へ）

```
INVITE sip:{{.PeerNumber}}@{{.Domain}} SIP/2.0
Via: SIP/2.0/UDP [{{.SelfV6}}]:5060;branch=z9hG4bK{{.Branch}};rport
Route: <sip:[{{.SIPServer}}]:5060;lr>
Max-Forwards: 70
From: <sip:{{.Extension}}@{{.Domain}}>;tag={{.FromTag}}
To: <sip:{{.PeerNumber}}@{{.Domain}}>
Call-ID: {{.CallID}}
CSeq: 1 INVITE
Contact: <sip:{{.ContactUser}}@[{{.SelfV6}}]:5060>
Allow: INVITE,CANCEL,ACK,BYE,UPDATE,PRACK
P-Preferred-Identity: <sip:{{.SelfNumber}}@{{.Domain}}>
Supported: 100rel,timer
Session-Expires: 300
Content-Type: application/sdp
```

**SDP オファー**

```
v=0
o=- {{.SessID}} 1 IN IP6 {{.SelfV6}}
s=-
c=IN IP6 {{.SelfV6}}
t=0 0
m=application {{.MediaPort}} udp octet-stream
b=AS:64
a=udp-setup:active
```

アンサーからは `c=` のアドレスと `m=` のポートを取る。ポート 0 は「このメディアを拒否した」の意なのでエラーにする。

### 5.6 メディア転送

```go
// 送信方向: tun -> 網
n, _ := tun.Read(buf)              // IFF_NO_PI なので生 IP パケットがそのまま入る
conn.WriteToUDPAddrPort(buf[:n], remote)  // 追加ヘッダなし

// 受信方向: 網 -> tun
n, src, _ := conn.ReadFromUDPAddrPort(buf)
if !acceptSource(src) { continue }        // 送信元検証
if ver := buf[0] >> 4; ver != 4 && ver != 6 { continue }
tun.Write(buf[:n])
```

- **ソケットは `connect()` しない。** 網内リレーが SDP と異なるポートから送ってくる可能性を考慮し、
  送信元 IP のみ照合し、ポートは最初に受けたものを学習して以降それに固定する
  （`--strict-peer-port` で厳密照合に切り替え可能）
- **DSCP**: SIP ソケットに EF、メディアソケットに CS1 を設定する
- **MTU**: 外側が IPv6(40)+UDP(8) なので `1500 - 48 = 1452` を既定とする
- **帯域**: `b=AS:64` は 64 kbps。超えて送ると網側で落とされる。現状 shaper は入れず、統計で見えるようにしている
- goroutine は方向ごとに 1 本ずつ。この帯域では性能上の工夫は不要なので、読みやすさを優先する

### 5.7 tun とネットワーク設定

tun の生成は ioctl 2 発（`IFF_TUN | IFF_NO_PI`）なので外部ライブラリ不要。
その後の MTU・アドレス・リンク up・経路は netlink で設定し、実施した操作をスタックに積んで逆順に戻す。

同名の tun が既に存在する場合は、**既定ではエラーにして中断**する（動いている別セッションを壊さないため）。
`--force` を指定したときだけ削除して作り直すが、その場合も型が tuntap でなければ拒否する。

権限は `CAP_NET_ADMIN`（`--bind-device` を使う場合は加えて `CAP_NET_RAW`）。

#### メディア宛先への経路

メディアの宛先（網内リレー）は、本来なら HGW が出す RA のデフォルト経路に乗るので、
アプリが経路を意識する必要はない。しかし**既にインターネット側の経路を持つ機器**では
HGW をデフォルトゲートウェイにできず（4.2 と同じ理由）、宛先だけが経路表から漏れる。
症状は確立直後の `sendto: network is unreachable` で、呼は張れているのに一切流れない。

「デフォルト経路に乗る」ことは実測で確認した。HGW の LAN 側 RA に入っているのは
オンリンクの `240b:10:dd83:c900::/64` を示す Prefix Information Option と非ゼロの
router lifetime だけで、**Route Information Option は無い**。つまり網内リレーへ届かせる
手段はデフォルト経路か宛先ごとの経路の二択しかなく、前者が使えない以上、後者を選ぶしかない。

そこで `--media-route auto`（既定）では、呼が確立してから宛先へのホスト経路を足し、
終了時に他のネットワーク設定と一緒に巻き戻す。設計上の要点は 3 つ。

- **足す前に必ず判定する。** `RTM_GETROUTE` を `oif=--bind-device` で引き、到達できるなら何もしない。
  メディアのソケットは `SO_BINDTODEVICE` で同じ IF に固定してあるので、判定もその条件に揃える。
  デフォルト経路が**別の**インタフェースにあるときに「到達できる」と誤判定しないため
- **網側のアドレス空間を決め打ちしない。** 宛先は 200 OK の SDP で確定するので、その `/128` だけを足す。
  `2404:1a8::/32` のようなブロックを実装に埋めると、網側の設備が増えたときに黙って壊れる
- **デフォルト経路には触らない。** 足すのはホスト経路 1 本だけで、既存の経路表は読むだけ

ゲートウェイはリースの SIP サーバ（= HGW）を既定にする。呼制御の宛先と同じ機器なので、
新しい設定項目を増やさずに済む。

### 5.8 確立後のコマンド実行

フラグの後ろに置いた位置引数をコマンドとして扱い、経路を入れて転送を始めた**あと**に起動する。
`--exec "..."` のような文字列フラグにせず argv をそのまま受けるのは、シェルの引用符に依存させないため
（シェルが要るなら `-- sh -c '...'` と書けばよい）。

**コマンドの寿命を呼の寿命に一致させる**のがこの機能の要点で、方向は双方向:

- コマンドが終了 → 終了理由 `command-exit` として即座に BYE を出す。
  「必要な間だけ呼を張る」が課金の観点で最も素直な運用になる
- 呼が先に終了（無通信・最大保持・相手 BYE・シグナル）→ コマンドに `SIGTERM`。
  5 秒（`WaitDelay`）で終わらなければ `SIGKILL`。トンネルの消えた呼を居座らせない

後始末の順序では**コマンドへの終了要求を BYE より前**に置くが、これはシグナル送出が非同期で
即座に返るため。「BYE を最優先」の原則は崩れない。プロセスの回収（`Wait`）は BYE と巻き戻しの後に行う。

プロセスグループは分けず、標準入出力もそのまま引き継ぐ。端末から使うときに `Ctrl-C` と
対話的なコマンドが自然に動くことを優先した（そのぶん `sh -c` の孫プロセスまでは面倒を見ない）。

終了コードはコマンドのものをそのまま ngntun の終了コードにする（シグナル終了は 128+シグナル番号）。
ただし**コマンドの終了が終了契機だった場合に限る**。呼が先に切れたときは ngntun 自身の理由のほうが
上位スクリプトにとって有用なので、§7 の終了コードを優先する。

---

## 6. 実機検証で分かったこと

実機で試して初めて分かったことが 4 つある。どれも症状から原因が見えにくい。

### 6.1 Vendor Class が内線番号割り当ての引き金

§4.1 のとおり。これを送らないとベンダオプションが返らない。

### 6.2 Contact の user 部・Allow・Supported: path を揃えないと INVITE が 488 で弾かれる

SDP の形式が正しくても通らない。HGW 自身が 1 ms で返してくる:

```
SIP/2.0 488 Not Acceptable Here
Warning: 304 Anonymous "Media type not available"
```

原因は SDP ではなくヘッダ側にあった。Contact の user 部にランダムな 12 桁の 16 進トークンを使い
（内線番号ではない）、あわせて `Allow: INVITE,CANCEL,ACK,BYE,UPDATE,PRACK` を、
REGISTER では `Supported: path` も送る。この 3 つを揃えたところ一発で確立した。

なお 3 つを同時に入れて通ったため、**どれが必須でどれが不要かは切り分けていない**。
1 つずつ戻して短い呼を出せば特定できる。

### 6.3 HGW は IPv6 の REGISTER に一切応答しない

送信元を委譲プレフィックス側にしても、HGW と同じ /64 のオンリンク側にしても無反応。
500 ms → 1 s → 2 s → 4 s → 4 s と 5 回再送してタイムアウトする。IPv4 なら 3 ms で 200 OK が返る。
`--register-transport ipv6` で追試できる。

### 6.4 多ホーム環境では経路表任せにできない

同じ /64 の直結経路が複数のインタフェースに存在すると、HGW 宛のパケットが別の
インタフェースへ出て無反応になる。このとき

- HGW の**リンクローカル**宛 ping → 応答あり
- HGW の**グローバル**宛 ping → 100% loss

という紛らわしい症状になる。`SO_BINDTODEVICE`（`--bind-device`）で送出インタフェースを
固定すれば、呼制御については経路表に手を入れずに解決できる。

同じ環境で**メディアだけが別の形で詰まる**。`SO_BINDTODEVICE` は送出インタフェースを固定するが、
経路が無ければ `sendto` は `network is unreachable` で落ちる。HGW をデフォルトゲートウェイに
できない機器では、呼は確立するのにメディアが一切流れない、という切り分けにくい状態になる。
`--media-route auto`（5.7）で宛先のホスト経路を呼の間だけ足すのが対処。

### 6.5 委譲プレフィックスが失効すると INVITE が黙殺される

HGW は、自分が委譲していないプレフィックスを送信元とする INVITE を**エラーも返さずに捨てる**。`100 Trying` すら来ないので、488 のように応答から原因を辿ることができない。

踏んだ状況は `dhclient -6` の二重起動で、Renew が通らず Rebind に落ちていた。手元のリースファイルには委譲プレフィックスが残っているので気づけないが、HGW 側では既に無効。同じタイミングの REGISTER は IPv4 で 200 OK が返り、`P-Associated-URI` まで正しいので、**登録はできるのに発信だけが無視される**という切り分けにくい形になる。

| 送信元 | 結果 |
|---|---|
| 失効したプレフィックス | 初回含め 7 回送信 → 32s でタイムアウト |
| 取り直したプレフィックス | `100 Trying` +12ms → `200 OK` +361ms |

内線番号は MAC / DUID に紐づくので Solicit をやり直しても安定するが、委譲プレフィックスはそのつど新しいものが払い出される。裏返せば古いほうは通知なく無効になる、ということ。4.3 で送信元をフラグに書かせずリースから導出しているのも、4.2 でアドレス付与をスクリプトに持たせたのも、この失効に追随させるため。

### 6.6 実装で踏んだバグ

**シグナル受信時にソケットが先に閉じられ、BYE と登録解除が送れない。**

`Transport.Serve` が `ctx.Done()` でソケットを閉じる作りだったため、SIGINT で ctx が
キャンセルされた瞬間に SIP ソケットが死んでいた。課金が止まるのは BYE が届いた時点なので、
Ctrl-C で切ったつもりが呼が張り付いたまま、という最悪の挙動になる。

ソケットを閉じる責務を `UA.Close()` に移し、ctx のキャンセルでは閉じないよう修正した。
回帰テストは `TestSocketsStayUsableAfterContextCancel`。

---

## 7. 異常系の扱い

| 事象 | 挙動 | 終了コード |
|---|---|---|
| リースの不備 | 起動時にエラー。不足項目をまとめて報告する | 2 |
| REGISTER が 401/407 | Digest 認証は未実装。明示的にエラー終了 | 7 |
| REGISTER タイムアウト | 中断 | 3 |
| INVITE が 4xx/5xx | 応答コードをログに出して終了。**自動リトライはしない** | 4 |
| 確立後にメディアが来ない | `--media-timeout` で BYE して終了 | 5 |
| 網から BYE | 正常終了扱い。統計を出して終了 | 0 |
| SIGINT / SIGTERM | BYE → 最大 2 秒待って cleanup | 0 |
| コマンドの起動失敗 | トンネルを維持する意味がないので直ちに BYE | 6 |
| コマンドの終了 | BYE して cleanup | コマンドの終了コード |

「自動リトライしない」は設計上の意思決定として明記しておく。
ダイヤルのリトライループはそのまま課金と網への迷惑につながるので、
判断は上位（systemd の `Restart=` や運用スクリプト）に委ねる。

**課金への配慮**として `--max-duration` は既定値を必ず持たせ、無制限をデフォルトにしない。

---

## 8. テスト

1. **ユニットテスト（ネットワーク不要）**
   - ベンダオプションのデコード: HGW が配布する実バイト列をゴールデンデータにする
   - SIP メッセージの解析・生成、SDP アンサーの解析
   - 確立後のコマンド: 終了コードの引き継ぎ、キャンセル時の `SIGTERM`、渡す環境変数
2. **ループバック結合テスト**
   - 擬似 HGW を localhost に立て、REGISTER → INVITE → ACK → BYE を実際に UDP で流す。
     488 応答時に ACK を返すか、網からの BYE でダイアログが終了するか、
     ctx キャンセル後もソケットが使えるか（§6.6 の回帰テスト）まで検証する
   - 擬似 tun と擬似リレーの間で双方向転送、送信元ポートの学習、壊れたパケットの破棄を確認
3. **実機テスト** — 課金の発生しない順に進める
   1. `--dry-run` で解決結果を目視確認（SIP を出さない）
   2. `--register-only` で登録だけ確認（発信しない）。内線番号が取れているかまで分かる
   3. `--max-duration 60s` の短時間で発信し、`--log-level trace` で SIP を全文ダンプして確認する
   4. ping もしくは実際に利用するプロトコルなどで疎通チェックを行う。
      接続確立後無通信の時間が続くと相手側から回線を切断される可能性があるため、**確立直後に流すこと** が望ましい。

---

## 9. まだ分かっていないこと

- メディアの許容 MTU の実測値（1452 が通るかは未検証）
- トンネル内に IPv6 パケットを流せるか
- `b=AS` に契約帯域より大きい値を書いたときの網の挙動
- 網側から re-INVITE / UPDATE によるセッションタイマ更新が実際にどう飛んでくるか
- Vendor Class の enterprise-number を別の値にしても内線が割り当てられるか
- 着信（`--answer`）は実装済みだが実機未検証
